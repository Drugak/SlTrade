import 'package:flutter/material.dart';

class ListOfBrokersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('List of brokers page'),
      ),
    );
  }
}